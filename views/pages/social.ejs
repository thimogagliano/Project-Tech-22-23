<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <header>
        <%- include('../partials/header') %>
    </header>
    <main>
        <section>
            <h3>Zoek naar andere gebruikers</h3>
            <p> zoek hieronder op gebruikers op basis van je eigen voorkeuren of kies uit bestaande gebruikers</p>
        </section>
        <section>
            <form>
                <label for="muziekgenres">Muziekgenres</label>
                <input type="checkbox" id="muziekgenres" name="muziekgenres">
                <label for="artiesten">Artiesten</label>
                <input type="checkbox" id="artiesten" name="artiesten">
                <label for="festivals">festivals</label>
                <input type="checkbox" id="festivals" name="festivals">
                <label for="voorkeuren">Voorkeuren</label>
                <input type="radio" id="voorkeuren" name="voorkeuren">
                <input type="submit" id="submit" name="submit">
            </form>
        </section>
        <section>
            <% users.forEach(user => { %>
                <h3><%= user.gebruikersnaam %></h3>
                <ul>
                    <li>Leeftijd: <%= user.leeftijd %></li>
                    <li>Genres: <% user.muziekgenres.forEach((genre, index) => { %>
                        <% if (index + 1 < user.muziekgenres.length) { %>
                            <%= genre + "," %>
                        <% } else { %>
                            <%= genre %>
                        <% } %>
                    <% }) %></li>
                </ul>
            <% }); %>
        </section>
    </main>
    <footer>
        <%- include('../partials/footer') %>
    </footer>
</body>
</html>